apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
# Prod overlay — placeholder for Phase 9
# Will add: prod ingress (mcp-gateway.uwcu.org), Entra ID config, resource scaling
